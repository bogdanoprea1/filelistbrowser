<Window x:Class="FilelistBrowser.test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="test">
    <Window.Resources>

    </Window.Resources>
    <Grid>
        <TabControl Style="{StaticResource tabControlStyle}" Margin="12,54,12,12" Name="tabControl1">
            <TabItem Style="{StaticResource tabItemStyleNoClose}">
                <TabItem.Header>
                    <Grid Height="10">
                        <Label Content="Search" Foreground="White" Margin="-8"></Label>
                    </Grid>
                </TabItem.Header>
                <TabItem.Content>
                    <Grid>
                        <TextBlock Text="test"></TextBlock>
                    </Grid>
                </TabItem.Content>
            </TabItem>

        </TabControl>
        <Button Height="23" HorizontalAlignment="Left" Margin="62,24,0,0" Name="button1" VerticalAlignment="Top" Width="118" Click="button1_Click">New Tab (small title)</Button>
        <Button Height="23" HorizontalAlignment="Right" Margin="0,25,80,0" Name="button2" VerticalAlignment="Top" Width="114" Click="button2_Click">New Tab (long title)</Button>
    </Grid>

</Window>
